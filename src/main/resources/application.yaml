ktor:
  deployment:
    port: 8081
  application:
    modules:
      - com.hivestudio.server.ApplicationKt.module

database:
  jdbcUrl: "jdbc:postgresql://localhost:5432/hive_studio"
  user: "postgres"
  password: "postgres"
  connectOnStartup: false
  showSql: false

storage:
  uploadDir: "./storage/uploads"
